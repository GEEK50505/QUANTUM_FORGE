# Environment configuration template for QUANTUM_FORGE
# Copy this to `.env` and fill in your Supabase credentials.

# Supabase project URL (example): https://xfkdjmfsdqarbkdctsxj.supabase.co
SUPABASE_URL=

# Supabase anon/public API key (short-lived for clients)
SUPABASE_KEY=

# Supabase service_role key (used server-side for storage uploads)
SUPABASE_SERVICE_ROLE_KEY=

# DATABASE_URL: IMPORTANT — Use the Supavisor Connection Pooler (Session Mode)
# Example (POOLER / Session Mode) — this resolves to IPv4-compatible endpoints and
# should be used by Docker/dev environments that lack IPv6 routing.
# Must use port 5432 on https://www.google.com/url?sa=E&source=gmail&q=.pooler.supabase.com domain for IPv4 support.
# Example value:
# DATABASE_URL=postgresql://postgres:MY_PASSWORD@aws-1-eu-west-2.pooler.supabase.com:5432/postgres
DATABASE_URL=

# Worker settings
WORKER_QUEUE=xtb_job_queue
WORKER_POLL_INTERVAL=1.0
WORKER_BUCKET=job_artifacts
